# This file was generated automatically from conda-smithy. To update this configuration,
# update the conda-forge.yml and/or the recipe/meta.yaml.
# -*- mode: yaml -*-

jobs:
- job: osx
  pool:
    vmImage: $(VMIMAGE)
  strategy:
    matrix:
      osx_64_blas_implblishdf51.14.6:
        CONFIG: osx_64_blas_implblishdf51.14.6
        UPLOAD_PACKAGES: 'True'
        VMIMAGE: macOS-15
        build_workspace_dir: ~/miniforge3/conda-bld
        free_disk_space: skip
        pagefile_size: 0
        resize_partitions: false
        store_build_artifacts: false
        tools_install_dir: ~/miniforge3
      osx_64_blas_implblishdf52:
        CONFIG: osx_64_blas_implblishdf52
        UPLOAD_PACKAGES: 'True'
        VMIMAGE: macOS-15
        build_workspace_dir: ~/miniforge3/conda-bld
        free_disk_space: skip
        pagefile_size: 0
        resize_partitions: false
        store_build_artifacts: false
        tools_install_dir: ~/miniforge3
      osx_64_blas_implmklhdf51.14.6:
        CONFIG: osx_64_blas_implmklhdf51.14.6
        UPLOAD_PACKAGES: 'True'
        VMIMAGE: macOS-15
        build_workspace_dir: ~/miniforge3/conda-bld
        free_disk_space: skip
        pagefile_size: 0
        resize_partitions: false
        store_build_artifacts: false
        tools_install_dir: ~/miniforge3
      osx_64_blas_implmklhdf52:
        CONFIG: osx_64_blas_implmklhdf52
        UPLOAD_PACKAGES: 'True'
        VMIMAGE: macOS-15
        build_workspace_dir: ~/miniforge3/conda-bld
        free_disk_space: skip
        pagefile_size: 0
        resize_partitions: false
        store_build_artifacts: false
        tools_install_dir: ~/miniforge3
      osx_64_blas_implopenblashdf51.14.6:
        CONFIG: osx_64_blas_implopenblashdf51.14.6
        UPLOAD_PACKAGES: 'True'
        VMIMAGE: macOS-15
        build_workspace_dir: ~/miniforge3/conda-bld
        free_disk_space: skip
        pagefile_size: 0
        resize_partitions: false
        store_build_artifacts: false
        tools_install_dir: ~/miniforge3
      osx_64_blas_implopenblashdf52:
        CONFIG: osx_64_blas_implopenblashdf52
        UPLOAD_PACKAGES: 'True'
        VMIMAGE: macOS-15
        build_workspace_dir: ~/miniforge3/conda-bld
        free_disk_space: skip
        pagefile_size: 0
        resize_partitions: false
        store_build_artifacts: false
        tools_install_dir: ~/miniforge3
      osx_arm64_hdf51.14.6:
        CONFIG: osx_arm64_hdf51.14.6
        UPLOAD_PACKAGES: 'True'
        VMIMAGE: macOS-15
        build_workspace_dir: ~/miniforge3/conda-bld
        free_disk_space: skip
        pagefile_size: 0
        resize_partitions: false
        store_build_artifacts: false
        tools_install_dir: ~/miniforge3
      osx_arm64_hdf52:
        CONFIG: osx_arm64_hdf52
        UPLOAD_PACKAGES: 'True'
        VMIMAGE: macOS-15
        build_workspace_dir: ~/miniforge3/conda-bld
        free_disk_space: skip
        pagefile_size: 0
        resize_partitions: false
        store_build_artifacts: false
        tools_install_dir: ~/miniforge3
  timeoutInMinutes: 360
  variables: {}

  steps:
  # TODO: Fast finish on azure pipelines?
  - script: |
      export CI=azure
      export CONDA_BLD_PATH=$(build_workspace_dir)
      export FEEDSTOCK_NAME=$(basename ${BUILD_REPOSITORY_NAME})
      export GIT_BRANCH=$BUILD_SOURCEBRANCHNAME
      export MINIFORGE_HOME=$(tools_install_dir)
      export OSX_FORCE_SDK_DOWNLOAD="1"
      export flow_run_id=azure_$(Build.BuildNumber).$(System.JobAttempt)
      export remote_url=$(Build.Repository.Uri)
      export sha=$(Build.SourceVersion)
      if [[ "${BUILD_REASON:-}" == "PullRequest" ]]; then
        export IS_PR_BUILD="True"
      else
        export IS_PR_BUILD="False"
      fi
      ./.scripts/run_osx_build.sh
    displayName: Run OSX build
    env:
      BINSTAR_TOKEN: $(BINSTAR_TOKEN)
      FEEDSTOCK_TOKEN: $(FEEDSTOCK_TOKEN)
      STAGING_BINSTAR_TOKEN: $(STAGING_BINSTAR_TOKEN)

  - bash: |
      conda_bld="$(build_workspace_dir)"
      conda_bld="${conda_bld/#\~/${HOME}}"
      if [[ -d "$conda_bld" ]]; then
        echo "##vso[task.setvariable variable=rehearsal_conda_bld]${conda_bld}"
        echo "##vso[task.setvariable variable=rehearsal_has_workspace]true"
      fi
    displayName: Locate rehearsal build artifacts
    condition: succeededOrFailed()
    continueOnError: true

  - task: CopyFiles@2
    displayName: Collect NRG packages and diagnostics
    condition: and(succeededOrFailed(), eq(variables['rehearsal_has_workspace'], 'true'))
    continueOnError: true
    inputs:
      SourceFolder: '$(rehearsal_conda_bld)'
      # work* includes conda-build's work_moved_* directories.
      Contents: |
        osx-*/nrgljubljana-*.conda
        osx-*/nrgljubljana-*.tar.bz2
        broken/nrgljubljana-*.conda
        broken/nrgljubljana-*.tar.bz2
        nrgljubljana_*/**/info/recipe/*.yaml
        nrgljubljana_*/work*/metadata_conda_debug.yaml
        nrgljubljana_*/work*/**/CMakeCache.txt
        nrgljubljana_*/work*/**/CMakeFiles/CMakeConfigureLog.yaml
        nrgljubljana_*/work*/**/CMakeFiles/CMake*.log
        nrgljubljana_*/work*/**/Testing/Temporary/*.log
        nrgljubljana_*/work*/**/Testing/Temporary/*.txt
        nrgljubljana_*/work*/**/Testing/**/Test.xml
      TargetFolder: '$(Build.ArtifactStagingDirectory)/$(CONFIG)/conda-bld'

  - task: CopyFiles@2
    displayName: Collect recipe provenance
    condition: succeededOrFailed()
    continueOnError: true
    inputs:
      SourceFolder: '$(Build.SourcesDirectory)'
      Contents: |
        recipe/meta.yaml
        recipe/build.sh
        .ci_support/$(CONFIG).yaml
        .ci_support/clobber_$(CONFIG).yaml
      TargetFolder: '$(Build.ArtifactStagingDirectory)/$(CONFIG)/feedstock'

  - bash: |
      artifacts="$(Build.ArtifactStagingDirectory)/$(CONFIG)"
      if [[ -d "$artifacts" ]] && [[ -n "$(find "$artifacts" -type f -print -quit)" ]]; then
        echo "##vso[task.setvariable variable=rehearsal_has_artifacts]true"
      fi
    displayName: Check for retained files
    condition: succeededOrFailed()
    continueOnError: true

  - task: PublishBuildArtifacts@1
    displayName: Retain rehearsal artifacts
    condition: and(succeededOrFailed(), eq(variables['rehearsal_has_artifacts'], 'true'))
    continueOnError: true
    inputs:
      PathtoPublish: '$(Build.ArtifactStagingDirectory)/$(CONFIG)'
      ArtifactName: 'rehearsal-$(CONFIG)-$(System.JobAttempt)'
